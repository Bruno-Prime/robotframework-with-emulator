#!/usr/bin/expect

set timeout -1
spawn ./tools/bin/sdkmanager --sdk_root=/home/runner/android-sdk --licenses
expect {
    "Accept? (y/N):" {
        send -- "y\r"
        exp_continue
    }
    eof
}
