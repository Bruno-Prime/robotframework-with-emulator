name: Testes de integração

on:
  workflow_dispatch:

jobs:
  testes:
    runs-on: self-hosted

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Install Dependencies
      run: pip3 install -r requirements.txt

#     - name: Start AppiumServer
#       run: |
#         chmod +x ./scripts/startAppiumServer.sh
#         ./scripts/startAppiumServer.sh
#       shell: bash

    - name: Run Tests
      continue-on-error: true
      run: |
        robot -d results .
      shell: bash

#     - name: Limpar Ambiente
#       run: |
#         rm /usr/lib/android-sdk/build-tools/* -f -r
#         rm /usr/lib/android-sdk/platforms/* -f -r
#       shell: bash

#     - name: Create AVD
#       continue-on-error: true
#       uses: reactivecircus/android-emulator-runner@v2.27.0
#       with:
#         api-level: 30
#         target: default
#         arch: x86
#         profile: Nexus 6
#         working-directory: ~/ANDROID
#         script: python3 -m robot -d results .

    - name: Upload Artifacts
      uses: actions/upload-artifact@v3
      with:
        name: resultados-testes
        path: results/
